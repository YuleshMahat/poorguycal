# PoorGuy Cal — support website

Small static site for app support and the privacy policy (React + Vite + TypeScript + Tailwind).

```bash
npm install
npm run dev    # local preview
npm run build  # output in dist/
```

Policy copy lives in **`PrivacyPolicy.md`** and is rendered on the `/privacy` route.
